# TODO - low-polly loading UI improvements

- [ ] Fix rendering spinner placement to be centered inside Output card
- [ ] Ensure when a new image is requested:
  - [ ] UI shows generating state in the center
  - [ ] previous image is not lost (or state preserved correctly)
  - [ ] after image load, swap in the new image
  - [ ] between images, prevent flicker (show generating UI)
- [ ] Remove unused imports/state and fix any TypeScript issues
- [ ] Run lint/build

